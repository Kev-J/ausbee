#

mainmenu "AUSBEE Configuration"

# Build options menu
menu "Build options"

config PROJECT_NAME
	string "Project name"
	default "project"
	help
		TODO

choice
	prompt "Select ARM core"
config ARM_CORE_CORTEX_M4
	bool "ARM Cortex-M4 core"

config ARM_CORE_CORTEX_M3
	bool "ARM Cortex-M3 core"
endchoice


config DEVICE_NAME # TODO Choice
	string "Device name"
	help
		TODO

config DOWNLOADS_DIR
    string "Downloads directory"
    default "$(CURDIR)/downloads"
    help
        TODO

endmenu

# Toolchain menu
menu "Toolchain"
source "Toolchain/Kconfig"
endmenu

# Packages menu
menu "FreeRTOS"
source "FreeRTOS/Kconfig"
endmenu
